/**
 * Dark theme constants
 */

const background = '#292929';
const accent = '#82ffb5';

const backgroundDark = '#131313';
const backgroundLight = '#333333';

const loadingBackgroundColors = [
  '#5cffa0',
  '#00B778',
  '#82ffb5',
];

const text = '#ffffff';
const subTitle = '#cccccc';

const theme: App.Theme = {
  accent,
  background,
  backgroundDark,
  backgroundLight,
  loadingBackgroundColors,
  subTitle,
  text,
};

export default theme;
